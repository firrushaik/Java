
Queue in Java:

In Java, the Queue is an interface in the java.util package that represents a collection designed for holding elements prior to processing. 
It follows the FIFO (First-In-First-Out) principle, meaning elements are added at the end and removed from the front. However, some implementations, like PriorityQueue, may process elements based on priority rather than insertion order.

It does not follow the insertion order.

example:

import java.util.PriorityQueue;
import java.util.Queue;

public class QueueEx{

    public static void main(String[] args){
        
        // Create a PriorityQueue of Integers
        Queue<Integer> pq = new PriorityQueue<>();
        
        // Adding elements to the PriorityQueue
        pq.add(50);
        pq.add(20);
        pq.add(40);
        pq.add(10);
        pq.add(30);
        
        // Display the PriorityQueue elements
        System.out.println("PriorityQueue elements: " + pq);
    }
}

o/p:

PriorityQueue elements: [10, 20, 40, 50, 30]



